# Run:
#  rm buf.lock && buf dep update && buf build
version: v2
name: buf.build/duckladydinh/giathuanapis
deps:
  - buf.build/googleapis/googleapis
lint:
  use:
    - DEFAULT
breaking:
  use:
    - FILE